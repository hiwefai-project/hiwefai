Download here the LPERFECT-M data

```bash

wget https://data.meteo.uniparthenope.it/extras/hiwefai/lperfectm-data.tar.gz --no-check-certificate

tar -xvzf lperfectm-data.tar.gz

rm lperfectm-data.tar.gz

```
