# LPERFECT-M data assets

Download and extract the LPERFECT-M data archive in this folder:

```bash
curl -L -o lperfectm-data.tar.gz \
  https://data.meteo.uniparthenope.it/extras/hiwefai/lperfectm-data.tar.gz

tar -xzf lperfectm-data.tar.gz
rm lperfectm-data.tar.gz
```
