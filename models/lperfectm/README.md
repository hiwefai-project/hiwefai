# LPERFECT-M model

Clone the LPERFECT-M repository into this directory:

```bash
git clone https://github.com/hiwefai-project/LPERFECT-M.git .
```

If you prefer cloning from the repository root, run:

```bash
git clone https://github.com/hiwefai-project/LPERFECT-M.git models/lperfectm
```
