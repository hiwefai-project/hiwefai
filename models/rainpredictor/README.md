# RainPredictor model

Clone the RainPredictor repository into this directory:

```bash
git clone https://github.com/hiwefai-project/RainPredictor.git .
```

If you prefer cloning from the repository root, run:

```bash
git clone https://github.com/hiwefai-project/RainPredictor.git models/rainpredictor
```
