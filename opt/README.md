# External utilities

This directory hosts the weather radar utilities used by the WebSocket server.

Clone the repository into `opt/weather-radar-utilities`:

```bash
git clone https://github.com/hiwefai-project/weather-radar-utilities.git opt/weather-radar-utilities
```
